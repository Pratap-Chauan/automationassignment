package automationassignment;

public class VariableNotation {
	
	
	int i = 10; 

	int int = 20; // Syntax error on token "int", invalid VariableDeclaratorId

	int goto = 30; //Syntax error on token "goto", invalid VariableDeclaratorId

	int default = 40; // Syntax error on token "default", invalid VariableDeclaratorId

	int class = 50; // Syntax error on token "class", invalid VariableDeclaratorId

	int j = 70;

	
	//So as we can see - i and j notation are correct 
}
